# Copy to /etc/sweetspot/.env on the VPS and fill in values.

# OpenAI Realtime
OPENAI_API_KEY=sk-...
VOICE=coral

# Supabase (service role — server-only!)
SUPABASE_URL=https://wsclhbwdsmlbzpkncgme.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# WhatsApp Business Cloud API (Meta Graph)
WA_PHONE_ID=
WA_TOKEN=
OWNER_MSISDN=447700900000

# Asterisk ARI
ARI_URL=http://127.0.0.1:8088
ARI_USER=sweetspot
ARI_PASS=change-me-strong-password

# Public host Asterisk reaches us on (loopback is fine since bridge runs on same VPS)
PUBLIC_HOST=127.0.0.1

# Asterisk ExternalMedia format (slin16 = PCM16 LE @ 16kHz)
AST_FORMAT=slin16
# Force outbound RTP payload type. Leave unset to learn from first inbound packet.
# Try 96 (dynamic) if Asterisk drops slin16 with PT=118.
RTP_PT=96