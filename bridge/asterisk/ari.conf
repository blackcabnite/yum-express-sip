[general]
enabled = yes
pretty = yes
allowed_origins = *

[sweetspot]
type = user
read_only = no
; password set in /etc/sweetspot/.env (ARI_PASS) — copy the same value below
password = REPLACE_WITH_ARI_PASS