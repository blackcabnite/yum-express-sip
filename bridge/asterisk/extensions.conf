[globals]

[default]

[whatsapp-inbound]
exten => _X.,1,NoOp(WhatsApp call from ${CALLERID(num)} to ${EXTEN})
 same => n,Answer()
 same => n,Stasis(sweetspot)
 same => n,Hangup()

exten => s,1,NoOp(WhatsApp call - no extension)
 same => n,Answer()
 same => n,Stasis(sweetspot)
 same => n,Hangup()